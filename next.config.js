/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['imagizer.imageshack.com', 'scontent.cdninstagram.com'],
  },
};

module.exports = nextConfig; 